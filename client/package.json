{
	"name": "client",
	"version": "1.0.0",
	"description": "Little man computer client",
	"main": "index.js",
	"scripts": {
		"build": "node prebuild.js && pnpm rimraf dist && pnpm tsc",
		"watch": "node prebuild.js && pnpm rimraf dist && pnpm tsc --watch",
		"test": "echo \"Error: no test specified\" && exit 1",
		"prettier:check": "prettier --check .",
		"prettier:write": "prettier --write ."
	},
	"author": "kiranvshah",
	"devDependencies": {
		"prettier": "^3.3.3",
		"rimraf": "^6.0.1",
		"typescript": "^5.5.3"
	}
}
